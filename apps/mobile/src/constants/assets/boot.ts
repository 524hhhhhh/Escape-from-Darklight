const BG_ASSETS = {
  LOADING: require("@assets/background/loading.png"),
};

const FONTS = {
  DEFAULT: require("@assets/font/Galmuri9.ttf"),
};

const COMMON_ASSETS = {
  HINT: require("@assets/hint/exit-hint-yellow.png"),
  LIGHT: require("@assets/light/light.png"),
};

export { BG_ASSETS, FONTS, COMMON_ASSETS };
